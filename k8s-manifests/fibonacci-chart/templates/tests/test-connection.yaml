apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fibonacci-chart.fullname" . }}-test-connection"
  labels:
    {{- include "fibonacci-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fibonacci-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
